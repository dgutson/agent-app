__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/

# Machine-local: permission allowlists carry absolute paths from one checkout.
.claude/settings.local.json

# Session scratch written by the handoff plugin, removed by /handoff:finish.
HANDOFF.md

# The linter's own findings, rewritten by every `--emit` run. Output, not
# source — and this repo lints itself on every change, so it is always here.
.agent-app-findings.json
